import React from 'react'
import * as Element from './styles'

const TelegramIcon: React.FC = () => {
  return (
    <Element.TelegramIconWrapper href="#">
      <Element.TelegramIcon />
    </Element.TelegramIconWrapper>
  )
}

export default TelegramIcon
